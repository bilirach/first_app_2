<table id="thetable" cellspacing="0" width="100%" >
  <thead>
  <tr>
    <th><%= sortable "first_name","Nom" %></th>
    <th><%= sortable "last_name","Prenom" %></th>
    <th><%= sortable "phone_number","Télephone" %></th>
    <th><%= sortable "description","Description" %></th>
  </tr>
  </thead>
<tbody>
<% @etudiants.each do |etudiant| %>
  <tr class="<%=cycle('even', 'odd')%>">
    <td><%= link_to etudiant.first_name, etudiant %></td>
    <td><%= etudiant.last_name %></td>
    <td><%= etudiant.phone_number %></td>
    <td><%= etudiant.description %></td>
    <!--<%= link_to 'Destroy', etudiant, confirm: 'Are you sure?', method: :delete %>-->
  </tr>
<% end %>
</tbody>
</table>
<div class="flickr_pagination">
    <!--<div class="page_info">
      <%= page_entries_info @etudiants %>
    </div>-->
<%= will_paginate @etudiants%>
</div>